import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: {
    template: "%s | EV Dealer Portal",
    default: "EV Dealer Portal",
  },
  description:
    "Premium EV Bike Dealer Management Portal — manage your electric vehicle inventory and dealer network with ease.",
  keywords: ["EV bikes", "electric vehicles", "dealer portal", "bike management"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
